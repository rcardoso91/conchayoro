jobs:
  jobx:
    runs-on: ubuntu-latest
    steps:
      - name: Update Node.js versions
        uses: hongaar/update-node-versions@v2.2.0

      - name: Verification of Node.js version
        run: node --version
